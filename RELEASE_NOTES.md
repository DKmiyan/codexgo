codexgo 0.1.12-preview.1 汇集近期的会话恢复、模型目录、图片与界面修复，供日常试用。它是预览版；稳定版仍为 0.1.11，正式 0.1.12 将在验收后另行发布。

改动：
- 改进终端交接的诊断与返回，修复 Pi 占用检查误匹配系统进程。
- 支持运行中引导，改进模型目录状态提示和菜单布局。
- 改进按模型能力处理图片、工具结果与宽表格显示。
- 增加插件验证入口、压缩事件显示及更新管理能力。

已验证：自动化协议／故障回归和构建检查；本机 Pi 的交接与续聊、基础引导、多模型菜单、图片识别、新宽表用例，以及用量与切模型窗口更新。

仍待验证／限制：Claude 和 CodeBuddy 的完整交互、远端场景、真实更新与回退、压缩前后显示尚未完整手测；远端客户端图片运输尚未支持。图片模型必须有账号权限，部分错误目前仍显示通用图片失败提示。不能把目录能列出模型视为账号已获授权。

安装与回退：下载对应平台压缩包和 SHA256SUMS，校验后解压到独立目录。待任务结束完全退出桌面，再从该目录启动；保留旧程序以便退出后切回。预览版不进入稳定自动更新渠道，后续 preview 仍需显式下载。适配器与主程序分开更新，活动会话不会自动热切换适配器。未自动更改现有安装、账号或会话数据。

发布包仅含可执行程序。Source code 自动归档只有下载说明元数据，不是应用源码。后续修复采用新的 preview 编号，不覆盖已有包。
