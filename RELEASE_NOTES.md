0.1.12-preview.2 更新 Claude 模型和 Effort 适配，并修复新会话状态、历史分页及工具消息显示。本版供日常试用，稳定版仍为 0.1.11；预览版不会被稳定自动更新安装。

## 主要变化

- Claude 模型和支持的 Effort 来自原生 CLI 目录，跟随原生 CLI 提供的新模型；实际可用性仍取决于账号权限。继承默认时明确显示“继承默认”，避免把默认档位误显示为固定数值。
- 新会话身份就绪后正确显示模型与状态；已成功获取的目录过期后继续作为缓存使用，减少误报待刷新。
- 修复适配器恢复连接时旧历史分页失效；附件列表查询返回兼容结果，无独立附件记录时返回空列表，聊天历史中的图片保留。
- Claude 后台任务通知不再作为用户消息出现。子代理工具显示摘要，并区分后台任务启动与实际完成；不支持原生标题同步的适配器跳过该操作。
- 恢复默认遇到不允许的跨 harness 模型时显示明确提示；该操作的原生意图区分尚未解决。

## 验证与限制

自动验证覆盖 Go race、66 项 DOM、45 项 Python，以及四种合成浏览器布局；四个平台已构建。以上不等于完整真实桌面、账号及远端验收。

仍在跟进：远端原生 CLI 安装器可能覆盖 codexgo 接管程序；Pi 原生会话回收站；原生“恢复默认”的意图区分；CodeBuddy 中断后的上下文保留。Claude 后台子任务的最终完成状态也未完整跟踪，不把“已启动”显示成“已完成”。

## 试用

从 Assets 下载对应平台的安装包与 SHA256SUMS，校验后解压到独立目录，保留稳定程序。当前任务结束并完全退出桌面版后，从新路径运行 `codexgo launch`。需要回退时，同样结束任务并退出后使用保留的旧程序启动。

更新主程序不会自动更新旧 Pi／CodeBuddy 适配器；请在插件设置核对目标主机并更新受管理适配器，手工配置先备份。已有会话固定使用启动时的适配器版本，重启和新会话后再验证。远端安装/更新前先核对接管路径，避免在活动会话中操作。

仅发布 macOS arm64/x86_64 和 Linux arm64/x86_64 安装包。GitHub 自动生成的 Source code 归档仅含公开下载元数据，不是应用源码或安装包。
