## 本次更新

- 精简输入区：保留原生模型、Effort 和上下文圆环，配置、累计用量与终端交接统一收进 ⋯。
- 新增插件设置入口，可查看本机和已登记远端的适配器、原生 CLI 与模型目录状态，并管理 Pi / CodeBuddy 适配器。手工注册保持原样，不自动覆盖。
- 持久化推理强度，同一 harness 内切换模型接续当前会话；保留运行状态与用时显示。
- 终端交接增加空闲检查、持久状态及明确返回流程；返回后重新打开会话读取最新历史。
- 加固多连接与审批归属、远端主机识别、历史分页、存储恢复和操作去重；修复 SSH 退出分类及尾部输出读取。
- 远端安装支持 Bash / zsh、事务回滚与经过认证的空闲停止；默认日志只记录摘要。

## 下载与升级

Apple Silicon Mac 下载 `codexgo_0.1.11_darwin_arm64.tar.gz`，Intel Mac 下载 `darwin_amd64`；Linux / WSL2 选择对应的 `linux_amd64` 或 `linux_arm64`。请同时下载 `SHA256SUMS` 并校验。

等待现有任务结束，完全退出桌面版，备份旧启动文件，再将新版 `codexgo` 放到原来的实际启动路径；运行 `version` 应显示 `0.1.11`，随后通过它重新启动。仅下载或替换文件不会升级正在运行的进程。

界面功能需在 `~/.codexgo/config.json` 合并 `"renderer_skin": true` 并重启。从齿轮或 ⋯ 的插件设置启用适配器；原生 CLI 的安装、登录与模型权限仍由各工具管理。旧 Pi / CodeBuddy 手工注册显示 `unmanaged` 时，请按安装说明核对 manifest 的 `exec` 是否指向刚更新的程序，不要直接删除已有配置。

[完整安装、升级和回退说明](https://github.com/DKmiyan/codexgo/blob/v0.1.11/README.md)

## 验证范围

Go race、27 项 DOM、17 项发布与文档测试，以及 macOS / Linux 四平台编译已通过；界面已用合成页面检查深浅色、窄窗口、键盘及 CodeBuddy 彩色图标。真实桌面、各账号模型权限、远端主机和终端往返仍需实际验收；iPhone 支持尚未确认。

旧 v0.1.10 远端 daemon 不支持新的停止握手，请按安装说明完成旧进程的退出与升级。CLI 返回桌面后需重新打开会话刷新历史；本版不支持跨 harness 接续。

安装包仅含可执行程序。GitHub 自动提供的 Source code 归档仅含公开下载元数据，不含项目源码。
